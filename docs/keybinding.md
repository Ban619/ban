This develop is practical uses, for those who are lazy enough to type in bit by bit. So I decided to create something that efficiently provides comfort for all.


The essential keys built-in for it are:

    alt + t = try
    alt + c = catch
    alt + f = finally
    alt + i = if
    alt + e = else
    alt + i e = elseif
    alt + s = switch
    alt + ctrl + f = 
    alt + ctrl + w =
    alt + ctrl + e =
    

